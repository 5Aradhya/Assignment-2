#include <stdio.h>
int main (){
    // Write a program to input three character from the user and display characters with their ASCII codes 
    char a ;
    printf(" Enter a character : ");
    scanf("%c",& a);
printf(" convet char to ASCII %c=%d",a,a);

}